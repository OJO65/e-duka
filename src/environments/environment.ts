export const environment = {
  production: false,
  // ── Point this at your Medusa backend (or any REST API) ──────────────────
  // During development, Medusa runs on port 9000 by default.
  // For the mock data server we'll set up, use port 3001.
  apiUrl: 'http://localhost:3001/api',
};
